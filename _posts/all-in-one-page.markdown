You can set the optimizers with `umx_set_optimizer()`

To access WLS fit, GenomicSEM, glm models etc. install GenomicMx `install.OpenMx("GenomicMx")`

To take advantage of multiple cores `umx_set_cores(n)`

## Learning

You can see inside models using `tmx_show(m1)`

 

